import { StyleSheet, Text, View } from "react-native";
import React from "react";
import SingularComponent from "./ParentComment";

const ReplyComponent = () => {
  return (
    <View>
    </View>
  );
};

export default ReplyComponent;

const styles = StyleSheet.create({});
